block variables
     
doctype html
html(lang="ru")
     head
          meta(charset='utf-8')
          meta(name="viewport" content="width=device-width, initial-scale=1")
          title #{title}
     body(class=pageClass)
        block content
        block scripts
